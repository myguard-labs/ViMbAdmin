SHELL=/bin/bash
MAILTO=someone@example.com


*/5 *	* * *	/usr/local/vimbadmin.git/bin/vimbtool.php -a archive.cli-archive-pendings
*/5 *	* * *	/usr/local/vimbadmin.git/bin/vimbtool.php -a archive.cli-delete-pendings
*/5 *	* * *	/usr/local/vimbadmin.git/bin/vimbtool.php -a archive.cli-restore-pendings

# Drain the mailbox-task queue (repair/optimize/archive/delete via doveadm HTTP).
# Each run processes up to queue.runner.max_per_run tasks, so a bulk action does
# not fire hundreds of doveadm calls at once.
*/2 *	* * *	/usr/local/vimbadmin.git/bin/vimbtool.php -a queue.cli-run


